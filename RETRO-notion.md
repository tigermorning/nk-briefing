# 뉴스레터 (북한 뉴스 주제) 회고 - 신서연

수업 다섯 단계(수집 → 선별 → 요약 → 검수 → 발행)를 LangGraph로 구현하고, 소스·선별 기준·발행 규칙을 북한 뉴스에 맞게 바꿨습니다. 매일 아침 GitHub Actions가 돌려 디스코드로 보냅니다.

- **독자**: 북한 소식을 매일 챙기지만 여러 매체를 돌 시간은 없는 한국어 독자
- **주기**: 매일 07:30 KST (`cron: 30 22 * * *`, UTC 기준)
- **발행**: 디스코드 embed 카드, 하루 3장 (심층·1차가 둘 다 있는 날만 4장)
- **칸 구성**: 1차(통일부) → 속보(연합·DailyNK 등) → 심층(38North·아시아프레스)
- **실행 기록**: 로컬 10회(실발행 1) + Actions 수동 2회, 모든 실행이 `store/metrics.jsonl`에 한 줄씩
- **저장소**: https://github.com/tigermorning/nk-briefing

---

## 1. 준비 — 소스 고르기 (관문 G1·G2·G3)

주제가 좁아서 "쓸 만한 소스가 있는가"부터 숫자로 확인했습니다.

| 관문 | 묻는 것 | 기준 |
|---|---|---|
| G1 | 원문 본문을 실제로 뽑을 수 있나 | 본문 600자 이상 |
| G2 | 양이 나오나 | 일간 칸 14일 7건 이상 / 주간 칸 14일 1건 이상 |
| G3 | robots.txt가 허용하나 | 허용 |

- **채택**: 연합뉴스 북한 · DailyNK · 데일리NK재팬 · RFA 한국어 · 38North(주간) · 아시아프레스(주간) · 통일부 북한동향 API(1차)
- **탈락**
  - NK News — 유료벽. 길이 기준(600자)은 5/5 통과했지만 본문이 아니라 티저였음
  - 연합뉴스 일문 — 한국어판 번역이라 새 사건이 없음
  - 중국신문망 국제 — 종합 피드 30건 중 북한 기사 1건
  - 시나 국제 — 최신 글이 2018년 (죽은 피드)
- **외국 후보 21곳 재측정**: 72시간 북한 기사가 핵심 3곳과 같은 사건인지 모델로 판정
  - 새 사건을 준 곳: 데일리NK재팬 3/4, NK News 2/5(티저라 요약 불가)
  - 0건: 신화망·TASS·인테르팍스·RIA·VOA 영문 등 9곳

**배운 점**
- **관문 통과가 쓸 수 있다는 뜻은 아니다.** 헛통과를 세 번 만났다.
  1. NK News 티저가 길이 기준 통과
  2. 연합 일문판이 양 기준 통과 (번역 중복은 G2가 못 봄)
  3. 통일부 상세페이지 73건 중 17건이 600자 통과 — 그런데 `본문 − 요약`이 73건 전부 **300~338자로 일정**. 새 글자는 0이고 페이지 장식만 붙은 것
- 그래서 **길이가 아니라 증가분**을 보도록 바꿨고, 티저는 `길이 편차(최장/최단)` + `문장이 중간에 끊겼는가` 두 신호로 가른다
- 첫 티저 판별기는 꼬리 문구(이메일·저작권)를 안 떼서 **정상인 연합·RFA를 5/5 티저로 오판**. 휴리스틱은 정상 소스에 먼저 돌려 봐야 한다

## 2. 자료 수집

- **수집 창**: 피드는 168시간으로 한 번만 받고, 속보는 24h → 48h → 72h 사다리를 메모리에서 적용
  - 24h에 5건 미만이면 창을 넓히고 카드 리드에 "사흘치"라고 밝힘
  - 72h에도 모자라면 **적게 낸다.** 숫자를 채우려 오래된 기사를 끼우지 않음
- **발행 원장** `store/published.json`: 창을 넓히면 어제 낸 기사를 다시 긁으므로, 실제로 보낸 링크만 기록하고 제외
- **통일부 API 1차 칸**: 원문이 없는 요약 전용 소스라 요약이 300자 이상인 날만 칸을 연다 → 30일 시뮬레이션에서 평일 21일 중 15일 열림
- **조용한 실패 가드** (에러 없이 결과만 틀리는 고장)

| 가드 | 잡는 것 |
|---|---|
| `200 + 빈 피드` → 오류로 승격 | 응답은 정상인데 글이 0건 |
| `SILENT` | 매일 나오던 소스가 24시간 기여 0 |
| `GAP` | 피드의 가장 오래된 글이 지난번 최신보다 새로움 = 사이 기사를 통째로 흘림 |
| `BELOW_BAR` / `NO_PUBLICATION` / `DEAD` / `STALE` | 1차 칸이 빈 이유를 한 상태로 뭉치지 않음 |

**배운 점**
- **피드의 창은 건수가 아니라 시간이다.** "DailyNK는 10건뿐이라 하루 1회면 넘친다"고 가정했는데, 재보니 10건이 78.9시간치(하루 3건). 가장 바빴던 24시간도 6건이라 1회/일로 충분
- 통일부 API가 계속 403이었는데, 키 활성화 대기가 아니라 **이중 인코딩**(인코딩된 키를 `requests`가 한 번 더 인코딩)
- 통일부 API는 **틀린 분류 코드와 빈 결과가 똑같은 응답**(`totalCount 0`)을 준다. 오타가 "오늘 발행 없음"으로 조용히 넘어갈 수 있음
- 주중에 발행이 비면 장애부터 의심했는데, 8/17은 **광복절 대체공휴일**이었다. 공휴일부터 확인할 것

## 3. 중요도 선별

- **구조**: 후보 40건 초과 시 예선(같은 크기 묶음으로 균등 분할) → 본선 → 코드로 소스당 3건 상한·같은 사건 제거
- **선별 기준** (`audience.yaml`, 기사 하나를 보고 예/아니오로 답할 수 있게)
  - 미사일 발사·핵시설 가동 같은 군사 행동이 실제로 있었나
  - 지도부 인사·정책·대외 노선 변화가 공식 발표로 확인됐나
  - 주변국 정부가 북한 관련 조치(제재·회담·합의)를 발표했나
  - 북한 내부 물가·식량·통제 상황이 새로 확인됐나
- **버릴 것**: 북한 매체 보도를 옮기기만 한 기사 / 해마다 반복되는 의례 보도 / 전망만 있는 분석 / 지도자 일가 추측 기사 / 날씨·여가 스케치 기사

**실험 (gpt-4.1-mini, 후보 12건·72시간 풀 44건 고정)**
- 독립 채점: 1점이 12건 중 5건. temperature 0에서도 2건 점수가 흔들림
- **한 화면 상대평가는 목록 순서에 흔들림**: 정순·역순 상위 5건 겹침 3/5, 같은 순위 0/5
  → 정순·역순 두 번 묻고 순위 점수를 합산하도록 변경
- 토너먼트 66쌍 양방향 일치 86% (AI 뉴스판 50%). 김정은 체중설 기사는 0승 꼴찌
- **연합 쏠림**: 72시간 44건 중 연합 32건. 묶음 10·20·40 모두 최종 5건이 전부 연합, 그 안에 같은 미사일 발사가 2~3건
  → 소스당 상한·중복 제거가 왜 필요한지 숫자로 확인

**배운 점**
- `event` 라벨을 "짧게"라고만 하니 **분야 이름**이 나옴(`북한 내부 생활 상황`). DailyNK의 서로 다른 3건이 같은 사건으로 잘려 5칸 중 3칸만 참 → "누가 무엇을 했다" 모양으로 고침
- 반대로 **같은 훈련이 라벨만 달라 둘 다 실림**(`4종 섞어쏘기` / `합동타격훈련`). 프롬프트 보강으로는 안 고쳐져서, 뽑힌 목록을 한 번 더 나란히 놓고 묶게 하는 `find_dupes` 추가
- **해 봤다가 뺀 것**: 공원 스케치 기사를 막으려고 "해당 기준 번호를 대라, 없으면 0"을 시킴 → 2회 모두 기준 4(내부 상황)를 댐. **모델은 자기가 고른 기사를 "해당 없음"이라 하지 않는다.** 버릴 것 목록에 스케치 기사 한 줄을 넣으니 후보에도 안 오름

## 4. 요약(취재)

- 본문은 `requests` + User-Agent로 받아 `trafilatura`로 추출, 600자 미만은 제외(1차는 300자)
- 토픽(군사·핵 / 대외·외교 / 대남 / 내부 정치)마다 데스크 지침을 따로 둠
  - 예: 대남 — "북한 측 표현은 따옴표로 묶어 누구의 주장인지 드러낼 것"

**배운 점**
- **일본어 원문이 일본어 요약으로 샘**: 시스템 지시에 "반드시 한국어"를 넣어도 데일리NK재팬 기사 **10회 중 9회 일본어**
  → user 메시지 맨 앞에 "아래는 일본어 기사 본문입니다… 한국어로만"을 붙이니 **0/10**
- 재요청 뒤에도 한국어가 아니면 전엔 그대로 발행됐음 → 제외하고 로그로 남기게 고침
- temperature 0에서도 헤드라인은 3회 동일, **요약 속 숫자 조합은 3회 중 2가지**
- `trafilatura.fetch_url()`은 기본 UA가 막히면 `None` → 본문 0자 → "본문 없는 소스"로 오판. 직접 받아서 넘겨야 함

## 5. 검수

- 원문 대조 LLM 판정. 북한 매체의 주장을 사실처럼 단정한 것도 문제로 봄

**실험 — 숫자를 조작한 가짜 요약 5건**

| 방식 | 결과 | 틀린 이유 |
|---|---|---|
| 숫자 대조(정규식) | **1/5** | 정상인데 불합격: 만 단위를 풀어 쓴 `35,000`, 전각 숫자 `５～６` / 가짜인데 합격: "20배"가 원문 `2023` 안의 부분 문자열 |
| LLM 검수(3회 다수결) | **5/5** | 배율 과장·뒤바꿈 모두 잡고, 표기 차이는 합격 |

**배운 점**
- 일본어 소스가 들어오면서 문자열 비교는 더 못 쓰게 됐다
- 첫 실험은 4/5였는데 **테스트가 모든 경우에 같은 헤드라인을 넣은 실수**였다. 판정기보다 테스트를 먼저 의심할 것
- 실제 실행 12회 동안 **불합격 0건**. 검수가 일을 잘한 건지 아무것도 안 보는 건지 지금은 구분할 수 없다

## 6. 발행 + 자동 실행

- **실발행**: 9/14 19:14 KST 로컬 1회, 속보 5 + 심층 1 → 디스코드 표시 확인
- **안전장치**
  - `DRY_RUN`은 `"0"`일 때만 보냄 (전엔 `"1"`일 때만 dry라서 `true`·오타가 실발송)
  - 원장은 **실제로 보낸 카드만** 기록. dry-run·한도로 뺀 카드는 내일 후보로 남음
  - 원장 커밋 → push 순서. 거꾸로 하면 Actions가 같은 카드를 다시 보냄
  - 전부 DEAD거나 초안이 0건인 날은 "기사 없음" + 초록불이 아니라 **실패 공지 + 빨간불**
- **디스코드 피드백 반영**: 6장은 많고 3번째 카드가 공원 스케치 기사였음
  → **하루 3장**, 심층·1차가 초안·검수에서 떨어지면 그 칸은 속보가 받음. 안 실린 기사는 `[예비]`로 내일 후보
- **Actions**: Secrets 3개 등록, 수동 dry-run 2회 초록불. 통일부 API는 러너 IP에서도 응답

**버그·삽질**
- **데일리NK재팬이 러너에서만 403**: UA 5종 × URL 4종 = 20조합 전부 403 + Cloudflare `Just a moment...`. 로컬은 20회 전부 200 → 데이터센터 IP 차단으로 판단, 우회하지 않고 Actions에서만 끔(`NK_SKIP_SOURCES`)
  - 매일 DEAD로 두지 않은 이유: **늘 켜진 경보는 진짜 피드가 죽은 날을 가린다**
- **원장 키 버그**: `link.split("?")[0]`이 통일부 `view.do?...&trendMngNo=134739` 주소를 전부 한 키로 뭉침 → `utm_*`만 떼도록 수정
- **같은 사건으로 뺀 기사가 다음 날 부활**: 48시간 창에서 새 기사로 보임 → 짝이 실제로 나간 경우 같이 원장에 기록
- 코드 리뷰(별도 리뷰어) 8건 반영, 가짜 모델·네트워크 테스트 12개 시나리오로 경로 확인

## 7. 운영 (지표 · 설정 분리)

- 매 실행 `metrics.jsonl`에 단계별 건수·소스별 기여·경보·소요 시간 한 줄
- `scorecard.py`로 소스별 기여·단계별 통과율 추이 그래프
- `audience.yaml`은 시작할 때 pydantic(`extra=forbid`)으로 검증 → 키 오타·색 형식이 **새벽 실행 중간이 아니라 시작 전에** 멈춤

**배운 점**
- **Actions dry-run의 지표는 저장소에 안 남는다**(커밋 단계를 건너뜀). 로컬 기록만 세서 첫 회고에 "RFA 11회 기여 0건"이라고 **틀리게 적었다**. 실제는 Actions 1회차에 RFA 기사가 뽑혔고, 지금까지 12회 중 2회

## 8. 실행 결과 (9/14 19:14 실발행)

```
① 수집   속보 24h 창 21건 · 심층 4건 · 1차 BELOW_BAR · 원장 제외 0건
② 선별   속보 21 → 5 · 심층 4 → 1 · 1차 0
   − 같은 사건 4건 제거 (묶음 재확인 3 · 라벨 일치 1)
③ 취재   본문 704 ~ 2,940자
④ 검수   6 → 6건
⑤ 발행   6건 · 보냄
```

카드 3장 규칙 반영 후 dry-run (19:57): 속보 12 → 4 · 심층 3 → 1 · 검수 5 → 5 · **발행 3장 + 예비 2**

---

## 9. 단계별 개선 방안

| 단계 | 지금 문제 | 개선안 | 잴 지표 |
|---|---|---|---|
| 수집 | 러너 IP 차단으로 소스 1곳 누락 / RFA 기여 12회 중 2회 | 자체 러너(집 PC) / "N주 기여 0이면 후보로 강등" 규칙 / 조선중앙통신 인용을 직접 받는 경로 | 소스별 주간 기여 건수 |
| 선별 | 72h 44건 중 연합 32건 쏠림 / 어제 낸 사건의 후속 기사는 링크가 달라 원장에 안 걸림 | 사건 단위로 먼저 묶고 대표 1건 선정 / 최근 발행 제목을 중복 판정에 같이 넘기고 새 내용이면 "후속" 표시 | 발행 소스 분포, 날짜 넘는 중복 건수 |
| 요약 | temperature 0에서도 숫자 조합이 달라짐 / 목록 밖 토픽(`대내 정치`) 생성 | 수치마다 원문 인용 구절 / 북한 매체 주장은 출처 필드 분리 / 토픽을 스키마 enum으로 | 인용 구절이 원문에 실제로 있는 비율 |
| 검수 | 12회 동안 불합격 0 — 판정기가 일하는지 모름 | **카나리아**: 가짜 요약(배율 과장·숫자 자리 바꾸기) 1건을 매 실행 섞고, 못 거르면 경보 | 카나리아 검출률 |
| 발행 | 실패 공지가 독자 채널로 감 / 사람 검토 없음 | 운영자 웹훅 분리 / 발송 전 승인 옵션 | 오발송 건수 |

## 10. 종합의견 — 북한 뉴스를 주제로 고른 것에 대해

- **양이 아니라 신뢰가 병목이었다.** 24시간 창에 속보 후보가 12~21건씩 들어오고 5건 미만인 창은 한 번도 없어서 수집량 걱정은 없었다. 대신 같은 사건을 여러 매체가 옮기고, 북한 매체 주장을 인용한 기사가 섞이고, 추측 기사가 끼어든다. 그래서 시간 대부분이 수집보다 **중복 제거·출처 구분·검수**에 들어갔다
- **"통과했다"를 믿지 않는 습관이 생겼다.** 길이 기준 통과(티저), API 200 응답(빈 결과), 검수 전원 합격(판정기 확인 불가)이 전부 같은 모양이었다. 에러 없이 결과만 틀리는 고장은 **단계별 건수와 사유를 매번 남기지 않으면 몇 주씩 안 들킨다**
- **가정은 재보면 자주 틀렸다.** "10건창이라 넘친다", "주중 공백은 장애다", "기준 번호를 대게 하면 거른다", "RFA는 기여 0" — 넷 다 재보고 뒤집혔다. 문서에 결론을 적기 전에 숫자를 먼저 볼 것
- 주제를 되돌릴 생각은 없다. 다만 1차 소스(통일부)가 요약뿐이라, **조선중앙통신 인용을 직접 받는 경로**를 찾는 게 다음 과제다

## 11. 다음에 할 일

- [ ] 9/15 07:30 첫 예약 실행 로그 확인 (데일리NK재팬 `OFF`, 경고, 원장 커밋)
- [ ] 검수 카나리아 구현 (우선순위 1)
- [ ] 1차 칸 OPEN 경로를 실데이터로 확인 (지금까지 가짜 테스트로만 봄)
- [ ] 날짜 넘는 중복 판정
- [ ] 몇 주 성적표로 소스 강등 규칙 정하기
